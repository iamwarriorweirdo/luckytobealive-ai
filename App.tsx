/**
 * DEPRECATED / ĐÃ NGƯNG SỬ DỤNG
 * 
 * Logic chính của ứng dụng hiện tại nằm ở:
 * -> app/page.tsx (Giao diện chính)
 * -> app/layout.tsx (Cấu trúc gốc)
 * 
 * File này được giữ lại (trống) để tránh lỗi import nếu còn sót lại references,
 * nhưng không nên chứa code thực thi.
 */
export default function App() {
  return null;
}
